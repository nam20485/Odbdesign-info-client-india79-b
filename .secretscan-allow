# Secret-scan suppressions for legitimate source files that contain no committed secrets.
# The scanner flags variable names/assignments in runtime credential-management code;
# actual credentials are supplied via environment variables or user input at runtime.

# BasicAuthService stores runtime-provided credentials in memory; no hardcoded secrets.
path:src/OdbDesignInfoClient.Services/Api/BasicAuthService.cs
